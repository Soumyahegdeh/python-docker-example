apiVersion: v1
kind: Service
metadata:
  name: python-docker-example-service
  labels:
    app: python-docker-example-service
spec:
  selector:
    app: python-docker-example
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30001
  type: NodePort